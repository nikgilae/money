# Roadmap

Статусы: ⬜ не начато · 🔄 в работе · ✅ готово

## Фаза 0 — Настройка проекта 🔄
- [x] Vite + React + TS (create-vite)
- [ ] ESLint + Prettier (ESLint есть, Prettier не настроен)
- [x] Tailwind CSS
- [x] Git-репозиторий, .gitignore
- [x] Базовая PWA-конфигурация (vite-plugin-pwa, manifest, иконки-заглушки)

## Фаза 1 — Ядро: доходы/расходы, категории, баланс ✅
- [x] Dexie-схема: Transaction, Category
- [x] CRUD для транзакций
- [x] CRUD для категорий (+ предустановленный набор дефолтных категорий)
- [x] Форма добавления/редактирования транзакции
- [x] Список транзакций (с фильтрами по дате/категории)
- [x] Расчёт текущего баланса
- [x] Unit-тесты на расчёт баланса

## Фаза 2 — Бюджеты и цели накоплений ✅
- [x] Dexie-схема: Budget, SavingsGoal
- [x] CRUD бюджетов по категориям + прогресс-бар (потрачено/лимит)
- [x] CRUD целей накоплений + визуализация прогресса
- [x] Уведомление о превышении бюджета (в UI)

## Фаза 3 — Повторяющиеся платежи ✅
- [x] Dexie-схема: RecurringRule
- [x] CRUD правил повторения
- [x] Логика автогенерации транзакций по nextDueDate
- [x] Экран предстоящих/просроченных платежей

## Фаза 4 — Аналитика и графики ✅
- [x] Сводка по месяцам (доходы/расходы/баланс)
- [x] Pie chart: расходы по категориям
- [x] Line/bar chart: динамика доходы/расходы во времени

## Фаза 5 — Полировка PWA 🔄
- [x] Тест офлайн-режима (выключить сеть, проверить работу) — проверено headless-браузером (отключение сети, reload, работа с IndexedDB) на прод-сборке
- [x] Иконки/splash screen под iOS (все нужные размеры)
- [ ] Проверка "Добавить на экран Домой" в Safari — **требует ручной проверки на реальном iPhone**, недоступно из sandbox-окружения без Safari/iOS
- [x] Стратегия кеширования service worker (generateSW, autoUpdate)

## Фаза 6.5 — Мультисчета и переводы ✅
(добавлено после MVP — обнаружено упущение: несколько счетов + переводы между ними)
- [x] Dexie: новая версия схемы + upgrade-функция (миграция существующих транзакций на дефолтный счёт "Основной")
- [x] Dexie-схема: Account
- [x] Transaction: добавить accountId, transferPairId, тип 'transfer'
- [x] CRUD счетов (создание/архивация/переименование)
- [x] Расчёт баланса по каждому счёту + общий баланс "Всего" (только по активным счетам)
- [x] UI: выбор счёта в форме транзакции
- [x] UI: форма перевода между счетами (откуда/куда/сумма) — создаёт пару transfer-записей
- [x] Переключатель "Все счета / конкретный счёт" в списке транзакций
- [x] Исключить transfer из аналитики доходов/расходов и из бюджетов
- [x] Обновить существующие unit-тесты на баланс (теперь с учётом счетов)
- [x] Тест миграции на реальных данных перед тем как деплоить обновление
## Фаза 6.55 — UI: карусель счетов 🔄
(доработка после 6.5 — согласовали паттерн: свайп-карусель, общая сумма закреплена сверху)
- [x] Account: добавить isPrimary, sortOrder
- [x] Настройки счёта: возможность назначить/сменить основной счёт (кнопка "Сделать основным" + стрелки ←/→ для ручного порядка в AccountManager)
- [x] Строка "Всего на счетах" — закреплена над каруселью, не скроллится вместе с ней, пересчитывается из суммы всех неархивных счетов
- [x] Карусель счетов на CSS scroll-snap (snap-x snap-mandatory, по одной карточке на всю ширину)
- [x] При открытии приложения карусель сразу открыта на isPrimary счёте (fallback — первый по sortOrder)
- [x] Dots-индикатор синхронизирован со скроллом карусели (пассивный, без клика — по согласованию)
- [ ] Тест свайпа на реальном iPhone, не только в девтулзах — momentum scroll и snap на iOS Safari часто ведут себя иначе, чем в эмуляции — **требует ручной проверки на реальном устройстве**, недоступно из sandbox-окружения

## Фаза 6.56 — Бюджеты и аналитика по счетам ✅
(добавлено по запросу — бюджеты сейчас общие по всем счетам, аналитика всегда агрегирует все счета без возможности посмотреть один конкретный)
- [x] Budget: добавить необязательное поле accountId (не задано — бюджет глобальный по всем счетам, как сейчас; задано — считается только по этому счёту)
- [x] BudgetForm: селектор счёта ("Все счета" по умолчанию + список активных счетов)
- [x] budgetProgress.ts: calculateSpent/calculateBudgetProgress учитывают accountId бюджета при подсчёте потраченного
- [x] BudgetList: показывать привязанный счёт у каждого бюджета (или "Все счета", если не задан)
- [x] Unit-тесты на calculateSpent/calculateBudgetProgress с учётом accountId
- [x] Вкладка "Аналитика": переключатель счёта (по образцу AccountSwitcher из списка транзакций) — "Все счета" по умолчанию + конкретный счёт
- [x] App.tsx: транзакции для аналитики фильтруются по выбранному счёту перед передачей в MonthlySummaryCard/ExpensesPieChart/IncomeExpenseTrendChart (сами компоненты не изменены — уже принимали список транзакций пропом)
- [x] Проверить, что переводы (transfer) корректно ведут себя в per-account аналитике конкретного счёта (исключаются из доходов/расходов по точному типу, фильтр по счёту это не ломает)
- [x] Побочно найден и исправлен баг: useTransactions не реагировал на изменение accountId (отсутствовал в deps useLiveQuery) — чинит и AccountSwitcher в списке транзакций из Фазы 6.5, и новый в аналитике

Фаза 6.6 — Экспорт/импорт данных (бэкап) ⬜ 
☐ Экспорт: сериализация всех таблиц (accounts, transactions, categories, budgets, savingsGoals, recurringRules) в один
JSON-файл
☐ В экспорт добавить schemaVersion и exportedAt — чтобы будущие версии приложения могли распознать старый
бэкап
☐ Кнопка “Экспортировать” в настройках — скачивание файла через Blob + <a download>
☐ Кнопка “Импортировать” — выбор файла, парсинг и валидация структуры/версии перед записью
☐ Явное предупреждение перед импортом: “текущие данные будут полностью заменены, действие необратимо” +
подтверждение
☐ Обработка невалидного/повреждённого файла — понятная ошибка, не молчаливый краш
☐ Напоминание о бэкапе — баннер, если с последнего экспорта прошло >30 дней (дата последнего экспорта — в
localStorage)
☐ Тест на реальных данных: экспорт → очистить IndexedDB вручную → импорт → сверить, что всё восстановилось один в
один 

## Фаза 6 — Деплой ⬜
- [ ] Деплой на Vercel/Netlify
- [ ] Тест на реальном iPhone
- [ ] Багфиксы по итогам теста

---
Обновляй чекбоксы по ходу работы — это твой источник правды о прогрессе, не CLAUDE.md.
